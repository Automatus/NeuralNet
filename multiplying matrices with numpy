import numpy as np

# this is not a neural network, this code is just to see the difference between using * and numpy dot

a = np.zeros((2, 2))
a[1] = 2
a[0] = 1
print("a =", a)

b = np.zeros((2, 2))
b[1] = 3
b[0] = 1
print("\nb =", b)

print("\na dot b =", a.dot(b))
print("\na * b =", a*b)
